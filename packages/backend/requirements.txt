# Person A (backend) Python dependencies.
# Install into a venv:  python -m venv .venv && source .venv/bin/activate && pip install -r packages/backend/requirements.txt

neo4j>=5.20,<6            # Neo4j driver — setup runner + GDS queries
cognee>=0.1.15            # self-improving child memory (Neo4j-backed)
python-dotenv>=1.0        # load repo-root .env in the setup runner
httpx>=0.27              # backend-side REST calls where needed

# Dev / test
pytest>=8.0
